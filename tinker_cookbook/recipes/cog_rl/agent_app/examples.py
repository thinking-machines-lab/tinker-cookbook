"""A few demo task prompts for the UI. Just prompts — the app has no answer key."""

EXAMPLE_PROMPTS = [
    "Emit the sum of the integers from 1 to 20 inclusive (a single number).",
    "Emit every prime number below 40, one per line, in increasing order.",
    "Emit the greatest common divisor of 48 and 36 (a single number).",
    "Emit the 12th Fibonacci number, where fib(0) = 0 and fib(1) = 1 (a single number).",
    'Emit how many vowels (a, e, i, o, u) appear in the lowercase word "tokenizer".',
    'Emit "yes" if the lowercase word "racecar" is a palindrome, otherwise emit "no".',
]
